# Smoke Wave Guide

Use `plan.yaml` as the machine-readable execution authority for this fixture.

## Wave 1: Smoke Lane

- control PR: `P99-01`
- lane `smoke` owns `P99-01`
- required contract: `contract_smoke_api`
- keep validation scoped to the shared helper scripts and their committed smoke outputs
